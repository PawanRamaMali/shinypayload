% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/payload.R
\name{payload_logs}
\alias{payload_logs}
\title{Get recent log entries}
\usage{
payload_logs(level = NULL, limit = 50, since = NULL)
}
\arguments{
\item{level}{Filter by log level (optional)}

\item{limit}{Maximum number of entries to return (default 50)}

\item{since}{Only return logs after this timestamp (optional)}
}
\value{
A list of log entries
}
\description{
Get recent log entries
}
\examples{
if (interactive()) {
  # Get last 20 log entries
  recent_logs <- payload_logs(limit = 20)

  # Get only error logs
  error_logs <- payload_logs(level = "ERROR")

  # Get logs from last hour
  recent_logs <- payload_logs(since = Sys.time() - 3600)
}
}
